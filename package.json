{
  "name": "love",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "serve": "vite preview",
    "push": "yarn build && git add . && git commit -m 'update' && git push",
    "deploy": "yarn push &&  git subtree push --prefix=dist origin dist"
  },
  "dependencies": {
    "lib-flexible": "^0.3.2",
    "moment": "^2.29.1",
    "vant": "^3.2.0",
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.3.0",
    "@vue/compiler-sfc": "^3.0.5",
    "autoprefixer": "^10.3.1",
    "node-sass": "^6.0.1",
    "postcss-pxtorem": "^6.0.0",
    "sass": "^1.38.0",
    "sass-loader": "^12.1.0",
    "vite": "^2.4.4"
  }
}
